/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // SSR obligatorio — no client-side only rendering
  // Cumple regla copilot-instructions: SSR en todas las páginas
};

module.exports = nextConfig;
