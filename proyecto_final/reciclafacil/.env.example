# Copia este archivo a .env.local y completa los valores reales.
# Nunca subas .env.local al repositorio (ya está en .gitignore).

# Cadena de conexión a MongoDB Atlas (uso futuro, no requerida para el demo local)
DATABASE_URL="mongodb+srv://usuario:password@cluster.mongodb.net/reciclafacil"
